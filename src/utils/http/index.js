import http from './http';

export const { request } = http;
export const { get } = http;
export const { post } = http;
export const deleteMethod = http.delete;
export const { put } = http;
export const { form } = http;

export default http;
